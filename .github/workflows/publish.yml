name: Publish to pub.dev

on:
  workflow_dispatch:

  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+'

jobs:
  publish:
    permissions:
      id-token: write
      contents: read

    uses: dart-lang/setup-dart/.github/workflows/publish.yml@v1
